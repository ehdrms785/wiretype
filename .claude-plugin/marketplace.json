{
  "name": "wiretype",
  "owner": {
    "name": "daro",
    "url": "https://github.com/ehdrms785"
  },
  "plugins": [
    {
      "name": "wiretype",
      "source": "./claude-plugin",
      "description": "API drift audit + MSW mock refresh — compare what your code believes about API responses with what the wire actually returned (deterministic verdicts via wiretype diff, localized reports), and reconcile existing MSW mock data with freshly recorded traffic behind an explicit approval gate."
    }
  ]
}
